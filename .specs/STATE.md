# Project State

## Decisions

| ID | Decision | Status | Date | Rationale |
| -- | -------- | ------ | ---- | --------- |
| AD-001 | Unity 6 (6000.x) + URP; validation target is the Unity Editor only (project stays iOS-exportable, no device build required) | active | 2026-08-17 | User decision during spec Q&A |
| AD-002 | Architecture: light MVC/MVP — plain-C# domain layer + C# `event Action`; no DI framework, no ScriptableObject event system | active | 2026-08-17 | User decision; keeps domain unit-testable in EditMode |
| AD-003 | Input via Input System (new) + EnhancedTouch; UI via uGUI (Canvas) + TextMeshPro | active | 2026-08-17 | User decision |
| AD-004 | Docs, code and UI language: English  | active | 2026-08-17 | User expressed no preference; consistency |
| AD-005 | Tests: Unity Test Framework — EditMode for pure logic, PlayMode for interaction | active | 2026-08-17 | User decision |
| AD-006 | P0 bootstrap assumes the Unity MCP is active for Editor operations. Fallback if the MCP is unavailable: the agent writes ALL text assets itself (Packages/manifest.json, ProjectSettings, scene YAML, asmdefs, scripts) and the human performs only the Editor-side verification steps (open project to import, run Test Runner, switch to iOS build target). The phase does NOT block waiting for the MCP | active | 2026-08-17 | Every P0 artifact is a text file Unity can import; only verification genuinely needs the Editor, so blocking on the MCP would be pure waste |
| AD-007 | Selection is single: `int? selectedSurfaceId` in RoomModel (no HashSet). `SelectionChanged` carries `(int? previous, int? current)`. Tapping the selected surface keeps it selected (no toggle-off, no event). Clear button is the ONLY path to the empty state (tap-outside-clears removed; CLR-02 retired). `ClearSelection()` idempotent: empty → no event, no state change | active | 2026-08-17 | User revision request 2026-08-17; (previous, current) lets views update both affected rows in one call |
| AD-008 | Domain concept renamed Wall → Surface with `SurfaceKind {Wall, Floor, Ceiling}`. Floor and ceiling are selectable like walls; windows can be cut ONLY into `Wall` surfaces (validator rejects others with `InvalidSurfaceKind`); window mode button visible only while a Wall is selected | active | 2026-08-17 | User revision request 2026-08-17 |
| AD-009 | Surfaces are solid slabs with serialized thickness (0.15 m default). SurfaceMeshBuilder emits front + back grid-sliced faces, outer slab sides, and 4 reveal faces per window opening. Every mesh rebuild (window add OR remove) reassigns the MeshCollider's sharedMesh in the same operation | active | 2026-08-17 | User revision request 2026-08-17; renderer-only updates leave ghost colliders (rays hitting invisible walls through openings) |
| AD-010 | Selection feedback: P1 requirement is tint via MaterialPropertyBlock only. Outline demoted to P2 polish (OUT-01) with an explicit stencil two-pass technique (stencil mark pass + edge pass via two RenderObjects features). When the outline layer swap is active, the selection raycast mask MUST include both default and `SelectedSurface` layers | active | 2026-08-17 | User revision flagged that a RenderObjects layer pass alone does not produce an outline; tint carries P1 risk-free |
| AD-011 | Window deletion is in scope (was Out of Scope): each opening gets its own BoxCollider; tap → "X" button anchored top-right of the opening; X → confirmation popup; confirm → remove window + rebuild mesh and collider. Window move/resize remains out of scope | active | 2026-08-17 | User revision request 2026-08-17 |
| AD-012 | View switch via "2D \| 3D" buttons at the top of the screen. Entering 2D: orthographic top-down camera; ceiling MeshRenderer AND MeshCollider disabled (renderer-only would leave an invisible collider blocking every plan tap); selection cleared and in-progress window draw cancelled. Returning to 3D: ceiling restored, camera placed at the room centre in Orbit mode. Camera-inside-room constraint scoped to Orbit mode only | active | 2026-08-17 | User revision request 2026-08-17 |
| AD-013 | ModeManager transition matrix documented in design.md: Orbit is the hub; WindowDraw and Ar are only enterable from Orbit (WindowDraw additionally requires a selected Wall); TopDown is enterable from any mode and cancels selection + draw (and ends the AR session); TopDown exits only to Orbit. Illegal transitions: `TrySet` returns false | active | 2026-08-17 | User revision item 14: `TrySet()` implied illegal transitions existed but none were documented |
| AD-014 | Window opening colliders are identified by their `WindowView` component at raycast time — no dedicated layer. A window hit routes the tap to the deletion UI (X) and NEVER changes the selection; SEL-03 reworded to "hits no surface" | active | 2026-08-17 | Grill: WIN-04's opening collider made SEL-03's "hits nothing" impossible once a window exists; user chose component/tag check over a layer |
| AD-015 | While in WindowDraw mode, surface taps are swallowed — the target wall is locked; selecting another surface requires exiting the mode. Window mode button visible only while (Wall selected AND mode == Orbit) | active | 2026-08-17 | Grill: a tap moving selection to Floor mid-mode would violate the WindowDraw entry invariant; visibility rule without the mode gate left a dead button in AR/2D |
| AD-016 | 2D plan usability: orthographic camera framed fit-to-room (+small margin, never further out); Floor hit within 30 px (tunable) of a wall selects the nearest wall; pinch zoom 0.5x–2.0x of fit-to-room, 2D only (3D orbital camera stays zoom-free per Out of Scope) | active | 2026-08-17 | Grill: walls are ~0.15 m edge-on in plan (~10 px on screen) — untappable without assist; user confirmed tolerance + 2D-only zoom |

## Handoff

**Last session:** 2026-08-17
**Feature:** room-wall-selection
**State:** Tasks phase complete (2026-08-17 evening): 29 atomic tasks across 5 phases (P0 bootstrap → domain → P1 presentation → P2 windows/outline → P3 AR/2D) in `.specs/features/room-wall-selection/tasks.md` (canonical: coverage matrix, gates, dependency diagrams, validation tables — `validate_tasks.py` passes with 0 errors) plus one detailed functional+technical doc per task in `docs/tasks/` (user-requested layout). 24/24 requirements mapped. Gates run via Unity MCP `run_tests` (fallback AD-006). Commit convention: user-global `[type] description` in English overrides check_commit.py. No code exists yet.
**Next step:** User approval of tasks.md (incl. provisional Test Coverage Matrix + Gate Check Commands), then Execute — 29 tasks pack into ~4-5 batches, so the sub-agent offer applies.
